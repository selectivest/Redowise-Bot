version: '3.8'

services:
  bot:
    build: .
    container_name: taskmanager-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./database:/app/database  # Persist database files
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge 